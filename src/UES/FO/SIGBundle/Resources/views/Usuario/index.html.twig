{% extends 'SIGBundle:Default:index.html.twig' %}
{% import _self as util %}
{% block content %}
<h1>Consultar usuarios</h1>
<table class="table striped">
    <thead>
        <tr>
            <th>Nombre completo</th>
            <th>Nombre de usuario</th>
            <th>Habilitado</th>
            <th>Operación</th>
        </tr>
    </thead>
    <tbody>
        {% for entity in entities %}
        <tr>
            <td>{{ entity.nombres ~ ' ' ~ entity.apellidos }}</td>
            <td>{{ entity.username }}</td>
            <td>{% if entity.enabled %}
            <i class="glyphicon glyphicon-ok-circle"></i>
            {% else %}
            <i class="glyphicon glyphicon-ban-circle"></i>
            {% endif %}
            </td>
            <td>
                <div class="btn-group btn-group-vertical">
                    <a href="{{ path('usuario_show', { 'id': entity.idusuario }) }}" class="btn btn-info btn-xs"><i class="glyphicon glyphicon-eye-open"></i> Consultar</a>
                    <a href="{{ path('usuario_edit', { 'id': entity.idusuario }) }}" class="btn btn-info btn-xs"><i class="glyphicon glyphicon-pencil"></i> Modificar</a>
                </div>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
<a class="btn btn-primary" href="{{ path('usuario_new') }}"><i class="glyphicon glyphicon-plus"></i> Ingresar nuevo usuario</a>
{% endblock %}


{% macro getNivel(rol) %}
    {% if rol == 'ROLE_OPERATIVE' %}
        Administración del sistema
    {% elseif rol == 'ROLE_TACTIC' %}
        Táctico
    {% elseif rol == 'ROLE_STRATEGIC' %}
        Estrátegico
    {% else %}
        Sin rol
    {% endif %}
{% endmacro %}

{% macro estado(estado) %}
    {% if estado %}
        <i class="glyphicon glyphicon-ok-circle"></i>
    {% else %}
        <i class="glyphicon glyphicon-ban-circle"></i>
    {% endif %}
{% endmacro %}